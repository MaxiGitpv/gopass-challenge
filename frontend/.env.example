# Local development (Vite proxy en vite.config.ts redirige /api → :3000, deja vacío)
VITE_API_URL=

# Producción Railway — reemplazar con la URL del servicio backend
# VITE_API_URL=https://gopass-api.up.railway.app/api
